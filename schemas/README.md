# Sanity Schemas

Define your Sanity content types here (for example `hero`, `marketStats`, `investmentTerms`, `pressPost`).

This project is already wired to consume content through `lib/sanity.ts`.
